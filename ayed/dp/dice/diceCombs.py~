MOD = 10**9 + 7

def solve(N):
    dp = [0] * (N + 1)

    dp[0] = 1

    for i in range(1, N + 1):
        for j in range(1, 7):
            if j <= i:
                dp[i] = (dp[i] + dp[i - j]) % MOD

    return dp[N]

N = 4
print(solve(N))
